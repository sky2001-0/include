## some usefuls for me

Supported only C++17 and later.

`namespace u` supports universal (fundamental) constants.

`namespace ps` supports constants specific to positronium.

`namespace csp` is a utility library.

`namespace csp::math` provides mathematic functions.

`namespace csp::utils` provides utility functions.
